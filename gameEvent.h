// gameEvent.h

#ifndef _GAMEEVENT_h
#define _GAMEEVENT_h

#if defined(ARDUINO) && ARDUINO >= 100
	#include "arduino.h"
#else
	#include "WProgram.h"
#endif

#include "variableType.h"

// gameEvent sis‰lt‰‰ n‰pp‰inten painallukset
struct gameEvent {
	
};

#endif

